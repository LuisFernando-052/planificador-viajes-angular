import { FechaLegiblePipe } from './fecha-legible-pipe';

describe('FechaLegiblePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaLegiblePipe();
    expect(pipe).toBeTruthy();
  });
});
